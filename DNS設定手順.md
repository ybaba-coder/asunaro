# 独自ドメイン切り替え手順（www.warms-isahaya.com）

## 入口：Google管理コンソール経由が確実です

1. https://admin.google.com にアクセス
2. Google Workspace の**管理者アカウント**でログイン
3. 左メニュー「ドメイン」→「ドメインを管理」
4. `warms-isahaya.com` の「ドメインの登録内容を管理」をクリック
   → Squarespace のドメイン画面が開きます（追加ログイン不要）
5. 「DNS」または「DNS Settings」を開く

---

## 作業① www を GitHub に向ける【必須】

**削除するレコード**

| ホスト | タイプ | 値 |
|--------|--------|-----|
| www | CNAME | ext-sq.squarespace.com |

**追加するレコード**

| ホスト | タイプ | 値 |
|--------|--------|-----|
| www | CNAME | ybaba-coder.github.io |

※ 値の末尾にピリオド（.）が自動で付く場合はそのままでOKです。

---

## 作業② www無しでもアクセスできるようにする【推奨】

`warms-isahaya.com`（www無し）で入力された時も表示されるようにします。

**削除するレコード（Aレコード4件すべて）**

| ホスト | タイプ | 値 |
|--------|--------|-----|
| @ | A | 198.185.159.144 |
| @ | A | 198.185.159.145 |
| @ | A | 198.49.23.144 |
| @ | A | 198.49.23.145 |

**追加するレコード（4件）**

| ホスト | タイプ | 値 |
|--------|--------|-----|
| @ | A | 185.199.108.153 |
| @ | A | 185.199.109.153 |
| @ | A | 185.199.110.153 |
| @ | A | 185.199.111.153 |

※「@」は画面によっては空欄、または `warms-isahaya.com` と表示されます。

---

## ⚠️ 絶対に触らないレコード

これらを消すと**メールが止まります**。

- **MX レコード**（`smtp.google.com`）… メール受信
- **TXT レコード**（SPF・DKIM・google-site-verification など）… メール認証

Webの設定（CNAME・A）とメールの設定（MX・TXT）は別物なので、
上記の CNAME と A だけ変更すればメールは影響を受けません。

---

## 設定が終わったら

「**DNS設定した**」とお知らせください。以下を私が行います。

1. 反映確認（10分〜数時間かかります）
2. GitHub側で独自ドメインを設定
3. HTTPS（鍵マーク）を有効化
4. canonical・sitemap を新URLに更新
5. 表示確認

そのあと Google Search Console への登録に進みます。

## 補足

- 切り替え後、Squarespaceの「近日中に公開」ページは表示されなくなります（意図どおりです）
- 切り替えが完了するまで、現在のURL https://ybaba-coder.github.io/asunaro/ はそのまま使えます
